name = "passqr-studio-worker"
main = "index.js"
compatibility_date = "2024-01-01"

# Scheduled cleanup — runs every hour
[triggers]
crons = ["0 * * * *"]

# Set secrets via: wrangler secret put PASSQR_API_KEY
# [vars]
# IOTPUSH_TOPIC = "Claude"
